// require with destructuring approach

// create function to display window

// listen on app 'ready' event

/*
    platform (OS) win32, darwin, linux
    app.on
    app.once

        triggering events
            before-quit
            window-all-closed
    
    isReady
    whenReady
    focus
    show
    hide
*/